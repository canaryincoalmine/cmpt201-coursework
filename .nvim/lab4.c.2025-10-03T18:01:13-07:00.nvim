#include <unistd.h>
#include <stdint.h>
#include <stdio.h>

struct header {
  uint64_t size;
  struct header* next;
};

int main() {
  void *current = sbrk(256);
  printf("%zu", sizeof(struct header));


  return 0;
}

